---
plan: <plan-name>
stream: <lang>
purpose: fixed-section-text
status: draft
---

# <Plan title> — `<lang>` fixed text

Every **Fixed** section declared in `About <plan-name>.md` has exactly one `##`
block here, keyed by its section id. The generator copies these blocks
**character-for-character** into every day file: no paraphrase, no reordering,
no punctuation drift, no added headings.

This file is the only place this text lives. It is never duplicated inside a
skill, a template or a day file's scaffold.

Each stream's Fixed text is its own attested text. Do **not** translate a Fixed
block from another stream — if this language has no attested version of a
liturgical block, say so and stop rather than producing one.

---

## `<section id>`

<Paste the fixed text here, exactly as attested. Keep the source's own line
breaks, punctuation and sub-headings.>

---

## `<section id>`

<…>
